# --- API / web framework -------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9      # required by FastAPI for file upload endpoints

# --- Schemas / config (see schemas.py, config.py) -------------------------
pydantic>=2.6
pydantic-settings>=2.2        # pulls in python-dotenv automatically for .env loading

# --- Vision-language models / deep learning -------------------------------
torch>=2.2
transformers>=4.39            # Donut and LayoutLMv3 both load through this
sentencepiece>=0.2.0          # required by several HF tokenizers (incl. Donut)
accelerate>=0.28              # speeds up / simplifies model loading across CPU/GPU
pytesseract>=0.3.10           # OCR — LayoutLMv3 needs text+bounding boxes as input, unlike Donut

# --- Prompted VLM fallback (schema-prompted extraction, no training) ------
openai>=1.14                  # swap/add groq, anthropic, etc. if using a different provider

# --- Document preprocessing ------------------------------------------------
pdf2image>=1.17               # requires the poppler system package installed separately (not pip)
pillow>=10.2
opencv-python>=4.9

# --- Storage / vector DB / RAG ---------------------------------------------
chromadb>=0.4.24
sentence-transformers>=2.6
langchain>=0.1.14
langchain-community>=0.0.31

# --- Dev / testing -----------------------------------------------------
pytest>=8.1
httpx>=0.27                   # used by FastAPI's TestClient for API tests